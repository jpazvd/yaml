# Fixture for REG-04: round-trip read/write
# Tests that yaml read followed by yaml write produces valid YAML

name: Round-Trip Test
version: 2.0

settings:
  debug: true
  timeout: 60

countries:
  - BRA
  - ARG
  - CHL

database:
  host: localhost
  port: 5432
  options:
    retries: 3
